{
  "name": "linceo-azure-extension",
  "version": "1.0.0",
  "description": "Extensión de Azure DevOps que ejecuta linceo como tarea nativa de pipeline.",
  "main": "index.js",
  "scripts": {
    "postinstall": "node scripts/install.js",
    "prebuild": "node scripts/install.js",
    "build": "node scripts/build.js",
    "prepackage": "npm run build",
    "package": "node scripts/package.js",
    "precreate": "npm run package",
    "create": "tfx extension create --output-path dist --rev-version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^4.9.5"
  }
}
